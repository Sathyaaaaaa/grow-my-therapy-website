/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'headshots-inc.com',
      'images.squarespace-cdn.com',
      'pexels.com',
      'images.pexels.com'
    ]
  }
};

module.exports = nextConfig;
